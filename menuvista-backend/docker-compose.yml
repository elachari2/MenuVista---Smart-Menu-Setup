name: menuvista

services:
  postgres:
    image: postgres:15-alpine
    container_name: menuvista_postgres
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER:-menuvista}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-menuvista123}
      POSTGRES_DB: ${DB_NAME:-menuvista_db}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-menuvista} -d ${DB_NAME:-menuvista_db}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: menuvista_redis
    restart: always
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
